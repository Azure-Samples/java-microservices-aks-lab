server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/slammini/spring-petclinic-microservices-config.git
          username: slammini
          password: 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
          default-label: main
        # Use the File System Backend to avoid git pulling. Enable "native" profile in the Config Server.
        native:
          searchLocations: file:///${GIT_REPO}

