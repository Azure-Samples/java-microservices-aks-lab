server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          https://github.com/wtomaz808/spring-petclinic-microservices-config.git
          username: wtomaz808
          password: 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
          default-label: main
        # Use the File System Backend to avoid git pulling. Enable "native" profile in the Config Server.
        native:
          searchLocations: file:///${GIT_REPO}

